# Design Choices

The system was designed with modularity, clarity, and reproducibility in mind.
Each vector database backend implements a shared interface, enabling fair comparison.
The RAG pipeline follows a standard structure: retrieval → context building → generation.

